# Node is used only for release tooling (pack compilation via foundryvtt-cli,
# migration smoke tests, the static audit). The system itself has zero build
# steps. Run `mise install` once, then `node tools/...` as usual.
[tools]
node = "24"
